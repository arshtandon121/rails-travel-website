<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
  body {
    font-family: 'Poppins', sans-serif;
    background-color: #1e1e1e;
    color: #fff;
  }
  .celebration-container {
    position: relative;
    overflow: hidden;
    background: black;
    border-radius: 15px;
    box-shadow: 0 10px 20px rgba(255,102,0,0.3);
    padding: 50px 0;
    color: #fff;
  }
  .confetti {
    position: absolute;
    width: 10px;
    height: 10px;
    background-color: #fff;
    animation: confetti-fall 3s ease-in-out infinite;
    opacity: 0.8;
  }
  .confetti:nth-child(odd) {
    background-color: #ff6600;
  }
  .confetti:nth-child(even) {
    background-color: #ff8c00;
  }
  @keyframes confetti-fall {
    0% { transform: translateY(-100px) rotate(0deg); opacity: 1; }
    100% { transform: translateY(100vh) rotate(720deg); opacity: 0; }
  }
  .card {
    transition: all 0.3s ease;
    transform-style: preserve-3d;
    background-color: rgba(255, 255, 255, 0.1);
    border: none;
    backdrop-filter: blur(10px);
  }
  .card:hover {
    transform: translateY(-10px) rotateX(5deg);
    box-shadow: 0 15px 30px rgba(255,102,0,0.3);
  }
  .status-icon {
    font-size: 72px;
    animation: pulse 2s infinite;
    color: #ff6600;
  }
  @keyframes pulse {
    0% { transform: scale(1); }
    50% { transform: scale(1.1); }
    100% { transform: scale(1); }
  }
  .card-header, .card-footer {
    background-color: rgba(255, 255, 255, 0.05);
    color: #fff;
  }
  .btn-primary {
    background-color: #ff6600;
    border-color: #ff6600;
  }
  .btn-primary:hover {
    background-color: #ff8c00;
    border-color: #ff8c00;
  }
  .btn-outline-primary {
    color: #ff6600;
    border-color: #ff6600;
  }
  .btn-outline-primary:hover {
    background-color: #ff6600;
    color: #fff;
  }
  .text-primary {
    color: #ff6600 !important;
  }
  .bg-primary {
    background-color: #ff6600 !important;
  }
  .list-group-item {
    background-color: rgba(255, 255, 255, 0.05);
    color: #fff;
    border-color: rgba(255, 255, 255, 0.1);
  }
  .background-element {
    position: absolute;
    background-color: rgba(255, 102, 0, 0.1);
    border-radius: 50%;
    z-index: -1;
  }
</style>
<title>Booking Confirmation</title>
</head>
<body>

<div class="celebration-container py-5">
<!-- Background elements -->
<div class="background-element" style="width: 300px; height: 300px; top: -150px; left: -150px;"></div>
<div class="background-element" style="width: 200px; height: 200px; bottom: -100px; right: -100px;"></div>
<div class="background-element" style="width: 150px; height: 150px; top: 50%; left: 10%;"></div>

<!-- Confetti -->
<div class="confetti" style="left: 10%; animation-delay: 0s;"></div>
<div class="confetti" style="left: 20%; animation-delay: 0.5s;"></div>
<div class="confetti" style="left: 30%; animation-delay: 1s;"></div>
<div class="confetti" style="left: 40%; animation-delay: 1.5s;"></div>
<div class="confetti" style="left: 50%; animation-delay: 2s;"></div>
<div class="confetti" style="left: 60%; animation-delay: 2.5s;"></div>
<div class="confetti" style="left: 70%; animation-delay: 3s;"></div>
<div class="confetti" style="left: 80%; animation-delay: 3.5s;"></div>
<div class="confetti" style="left: 90%; animation-delay: 4s;"></div>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <div class="card shadow-lg">
        <div class="card-header text-center py-4">
          <h2 class="mb-0 text-primary">Your Adventure Awaits!</h2>
        </div>
        <div class="card-body">
          <div class="text-center mb-4">
            <i class="fas fa-campground status-icon"></i>
            <h3 class="mt-3 text-white">Booking Confirmed!</h3>
            <p class="text-white">Get ready for an amazing experience!</p>
          </div>

          <div class="card mb-4 border-0 shadow-sm">
            <div class="card-body">
              <h4 class="card-title text-primary">Booking Details</h4>
              <ul class="list-group list-group-flush">
                <li class="list-group-item d-flex justify-content-between align-items-center">
                  Booking ID
                  <span class="badge bg-primary rounded-pill"><%= @booking.id %></span>
                </li>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                  Check-in Date
                  <span><%= @booking.booking_details['check_in_date'] %></span>
                </li>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                  Check-out Date
                  <span><%= @booking.booking_details['check_out_date'] %></span>
                </li>
                <% if @show_data.include?("per_person_field") %>
                  <li class="list-group-item d-flex justify-content-between align-items-center">
                    Number of Persons
                    <span><%= @booking.booking_details['number_of_persons'] %></span>
                  </li>
                <% end %>

                <% if @show_data.include?("per_km_field") %>
                  <li class="list-group-item d-flex justify-content-between align-items-center">
                    Distance
                    <span><%= @booking.booking_details['selected_distance'] %> km</span>
                  </li>
                <% end %>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                  Total Price
                  <span class="text-success">₹<%= number_with_delimiter(@booking.booking_details['total_price']) %></span>
                </li>
              </ul>
            </div>
          </div>

          <% if @show_data.include?("sharing_fields") %>
            <div class="card mb-4 border-0 shadow-sm">
              <div class="card-body">
                <h4 class="card-title text-primary">Your Accommodations</h4>
                <div class="row text-white">
                  <div class="col-md-3 text-center">
                    <i class="fas fa-bed fa-2x mb-2 text-info"></i>
                    <h5>Double Sharing</h5>
                    <p class="lead"><%= @booking.booking_details['double_sharing'] %></p>
                  </div>
                  <div class="col-md-3 text-center">
                    <i class="fas fa-users fa-2x mb-2 text-warning"></i>
                    <h5>Triple Sharing</h5>
                    <p class="lead"><%= @booking.booking_details['triple_sharing'] %></p>
                  </div>
                  <div class="col-md-3 text-center">
                    <i class="fas fa-home fa-2x mb-2 text-success"></i>
                    <h5>Quad Sharing</h5>
                    <p class="lead"><%= @booking.booking_details['quad_sharing'] %></p>
                  </div>
                  <div class="col-md-3 text-center">
                    <i class="fas fa-hotel fa-2x mb-2 text-danger"></i>
                    <h5>Six Sharing</h5>
                    <p class="lead"><%= @booking.booking_details['six_sharing'] %></p>
                  </div>
                </div>
              </div>
            </div>
          <% end %>

          <div class="card border-0 shadow-sm">
            <div class="card-body">
              <h4 class="card-title text-primary">Contact Information</h4>
              <ul class="list-group list-group-flush">
                <li class="list-group-item"><strong>Name:</strong> <%= @booking.name %></li>
                <li class="list-group-item"><strong>Email:</strong> <%= @booking.email %></li>
                <li class="list-group-item"><strong>Phone:</strong> <%= @booking.phone %></li>
              </ul>
            </div>
          </div>
        </div>

        <div class="card-footer text-center py-3">
          <% if @booking.user_id.present? %>
            <%= link_to "View All My Bookings", bookings_path, class: "btn btn-primary btn-lg" %>
          <% else %>
            <p>Create an account to manage your bookings easily!</p>
            <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-success btn-lg" %>
          <% end %>
          <%= link_to "Explore More Camps", camps_path, class: "btn btn-outline-primary btn-lg" %>
        </div>
      </div>
    </div>
  </div>
</div>

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
